<p id="notice"><%= notice %></p>

<h1>Breweries</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Year</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @breweries.each do |brewery| %>
      <tr>
        <!-- Changing default 'Show' to what it is to make comprehension easier -->
        <td><%= link_to brewery.name, brewery_path(brewery.id) %></td>
        <td><%= brewery.year %></td>
        <!-- <td><%= link_to 'Show', brewery_path(brewery.id) %></td> -->
        <td><%= link_to 'Edit', edit_brewery_path(brewery) %></td>
        <td><%= link_to 'Destroy', brewery, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>Amount of breweries &ndash; <b><%= @breweries.count %></b></p>

<!-- beers_path, new_beer_path yms yleisesti käytössä railsin magian avulla?? -->
<!-- beers_path, new_beer_path yms yleisesti käytössä railsin magian avulla?? -->
<%= link_to 'New Brewery', new_brewery_path %> |
<%= link_to 'Beer List', beers_path %>
